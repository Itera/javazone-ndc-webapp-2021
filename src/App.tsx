import { RouteListener } from './routes/RouteListener';
import { Router } from './routes';

function App() {
  return (
    <main>
      <RouteListener />
      <Router />
    </main>
  );
}

export default App;
